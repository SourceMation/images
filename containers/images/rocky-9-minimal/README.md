# SourceMation Rocky Linux 9 minimal image

Rocky Linux 9 minimal made by SourceMation team.


**MINIMAL IMAGES USES MICRODNF INSTEAD OF DNF/YUM** Please use `microdnf` instead of `dnf` or `yum` in minimal images.
